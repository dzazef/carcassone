import React, {Component} from 'react';
import './lobby.css'

class Lobby extends Component {
    render() {
        return (
            <div className={"m-lobby-root"}>

            </div>
        );
    }
}

export default Lobby;